#have to use capital letters 

source "https://rubygems.org"

gem "activesupport"
gem "awesome_print"
gem "pry-byebug"


# run the command "bundle install" > will help you then install all of those - documents the version of the gems >> will always grab the "Gemfile" that is why it is capitalized 
